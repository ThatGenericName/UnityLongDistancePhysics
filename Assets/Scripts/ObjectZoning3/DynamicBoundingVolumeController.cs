namespace ObjectZoning3
{
    public class DynamicBoundingVolumeController
    {
        public const float BaseOffset = 10;
        public const float HalfOffset = BaseOffset / 2;
        public const float QuarterOffset = BaseOffset / 4;
    }
}